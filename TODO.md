# OpenArm 项目统一待办

更新日期：2026-07-23

本文件是项目唯一的待办清单。后续完成一项就把对应的 `[ ]` 改成
`[x]`，避免在聊天记录和多个文档之间重复维护。

## 当前已经完成

- [x] TeleopXR、PyRoki 和 OpenArm 模型源码放入本项目。
- [x] 项目使用一个名为 `lerobot` 的 Conda 环境。
- [x] Pico 通过 USB/ADB 有线连接。
- [x] Pico 本机投屏，并以单只右眼画面全屏显示。
- [x] Pico 进入 TeleopXR，能够控制虚拟 OpenArm。
- [x] Pico 位姿经过 PyRoki 生成双臂 IK 关节目标。
- [x] IK 输出转换成 LeRobot 双臂 OpenArm 标准 action。
- [x] LeRobot 数据采集入口和数据字段已经搭好。
- [x] 双侧握键作为遥控保持开关：同时按下才控制，松开任意一侧停止
  发送新动作并保持当前位置。
- [x] 任意手柄位姿缺失时停止控制；重新使能时重建相对位姿基准。
- [x] IK 已有软关节限位和自碰撞约束。
- [x] LeRobot 发送动作时已有单次约 `2°` 的相对目标限制。
- [x] `teleop` 和 `ik` 模式不会连接 CAN；只有明确使用 `robot` 或
  `record` 模式才连接真机。

## 下一阶段：真机遥控前必须完成

### 1. 硬件参数核对

- [ ] 确认当前 OpenArm 机械版本与项目使用的 URDF 完全一致。
- [ ] 核对左右臂 CAN 接口、所有电机型号、发送 ID 和反馈 ID。
- [ ] 核对左右臂各关节方向、零点偏置和机械限位。
- [ ] 核对左右夹爪方向、开合范围和扳机映射。
- [ ] 单独排查旧工程曾出现的右臂 J6 反馈缺失问题。
- [ ] 将最终核对结果固化到配置文件，不能只保留在旧工程或聊天记录。

### 2. 安全控制

- [x] 增加 Pico 数据短时超时看门狗；控制期间超过 `0.25 s` 无新动作，
  自动重复失能双臂。
- [ ] 增加 WebSocket 断开、跟踪丢失和重定位后的明确安全状态。
- [ ] 增加独立急停和急停锁定；急停后必须人工确认才能恢复。
- [x] 增加基于真实 `dt` 的关节速度限制。
- [x] 增加基于真实 `dt` 的关节加速度限制。
- [ ] 处理 J1 微动的静摩擦释放：`5°` 往返测试的位置差实测速率达到
  `12.102°/s` 并触发安全失能，确认不只是电机速度反馈噪声。
- [ ] 增加 IK 关节目标跳变检测；异常时保持上一条安全目标。
- [x] 进程正常退出、`SIGTERM`、`SIGHUP`、控制线程异常和持续反馈超时均
  重复失能双臂并关闭力矩；正常松开侧握仍保持当前位置。
- [x] 左右臂分别统计反馈和发送异常；单侧 CAN 失败时，另一侧仍独立执行
  双重失能确认。
- [x] 增加 `DISABLED_CONFIRMED / ENABLED / UNKNOWN` 安全状态，以及超时、
  电机状态码、原始反馈帧和 SocketCAN 诊断日志。
- [x] 使用 FD-capable SocketCAN 接收电机 CAN-FD 反馈，所有电机命令统一为
  已验证的经典 8 字节帧；拒绝 LeRobot 0.6.0 的 FD-without-BRS 路径。
- [x] 增加硬件进程互斥和 4443 端口前置检查；冲突时在打开 CAN 前退出。

### 3. 控制效果

- [ ] 配置并验证 Pico 位姿滤波，消除静止抖动。
- [ ] 标定 Pico 到机器人坐标系的方向、原点和工作空间缩放。
- [ ] 验证双臂平移、旋转、肘部冗余姿态和夹爪动作方向。
- [ ] 验证连续使能时 IK 冗余参考不会逐帧漂移。
- [ ] 测量实际控制频率、延迟和抖动，排除旧工程约 21 Hz 的阻塞问题。

### 4. 分级真机验证

- [x] 只读连接 `can0`、`can1`，确认 16 个电机均有反馈，不发送动作。
- [ ] 架空或可靠固定机械臂，准备物理断电措施。
- [x] 确认 LeRobot 的电机使能和 MIT 控制帧能被当前固件执行；左臂 J1
  完成 `1.0°` 小目标往返测试，实际峰值位移 `0.328°`，并安全回到原位。
- [x] 正式 LeRobot 连接已改为不校准、不重设零点、不自动使能；首次有效
  遥控动作到来时才在实时读取的当前位置使能。
- [ ] 以最低速度逐关节验证单只左臂。
- [ ] 以最低速度逐关节验证单只右臂。
- [ ] 低速验证双臂和夹爪。
- [ ] 验证松开侧握、Pico 断开、网络断开、跟踪丢失、CAN 异常、
  `Ctrl+C` 和急停时的行为。
- [ ] 完成以上项目后，才允许正常执行 `./scripts/start.sh robot`。

## 数据采集

- [ ] 选择并配置实际使用的相机、分辨率、帧率和设备 ID。
- [ ] 验证数据帧记录的是机械臂实际回传状态，而不是 IK 目标。
- [ ] 验证 action 是经过转换、限位和安全检查后的最终实际下发值。
- [ ] 检查相机、关节反馈和 action 的时间戳及帧对齐。
- [ ] 确定 episode 开始、暂停、结束、成功和失败的标记方式。
- [ ] 验证磁盘写入异常不会阻塞机械臂控制。
- [ ] 安全验证完成后录制 5 至 10 个试验 episode。
- [ ] 使用当前锁定版本的 LeRobot 读取、检查并回放试验数据。
- [ ] 检查数据目录空间占用，并确定车载电脑的正式数据保存位置。

## 训练、推理和部署（遥控及采集稳定后再做）

- [ ] 确定训练电脑和车载电脑各自的硬件配置。
- [ ] 最后再决定是否升级 NVIDIA 驱动，以及对应的 CUDA、PyTorch 版本。
- [ ] 选择首个 LeRobot 策略并完成训练配置。
- [ ] 训练一个小数据集，验证完整训练流程。
- [ ] 将模型推理 action 接入与遥控相同的安全控制入口。
- [ ] 先离线评估，再低速真机评估。
- [ ] 在车载电脑上从干净系统执行安装脚本，验证可以一次部署成功。
- [ ] 固定全部依赖版本和外部源码 commit；补齐 PyRoki 上游版本记录。
- [ ] 编写车载电脑启动、停止、日志和故障恢复说明。

## 测试与工程收尾

- [ ] 为数据超时、IK 失败、关节限位、反馈缺失和安全退出增加自动测试。
- [ ] 为 Pico 坐标映射、侧握保持、夹爪映射和动作单位转换增加测试。
- [x] 增加不连接 CAN 的模拟回归测试，覆盖经典帧、状态码、双重失能确认、
  单侧总线故障、首帧看门狗和关闭竞态。
- [ ] 清理 README 中不存在的 Pico Wi-Fi 说明链接，或补齐对应文档。
- [ ] 真机参数确定后同步更新 README、硬件配置和数据采集说明。
- [ ] 保留旧工程为只读参考；确认新工程完全替代后再决定是否归档。
