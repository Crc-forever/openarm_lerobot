[project]
name = "openarm-teleop-env"
version = "0.1.0"
description = "CPU-only Pico TeleopXR input runtime (IK is added later)"
requires-python = "==3.12.*"
dependencies = [
  "numpy==2.2.6",
  "teleop-xr==1.3.5",
]

[tool.uv]
package = false
environments = ["sys_platform == 'linux' and platform_machine == 'x86_64'"]
