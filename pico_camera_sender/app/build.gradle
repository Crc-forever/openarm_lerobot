plugins {
    id "com.android.application"
}

android {
    namespace "com.openarm.picocamera"
    compileSdk 35

    defaultConfig {
        applicationId "com.openarm.picocamera"
        minSdk 30
        targetSdk 34
        versionCode 1
        versionName "0.1.0"
    }
}
